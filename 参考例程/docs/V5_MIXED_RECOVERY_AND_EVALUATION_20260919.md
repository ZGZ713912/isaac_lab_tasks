# 旧mixed停止、回收和同场景对照

## 已正常回收

按用户指令，2026-09-19 22:46:52北京时间核对run目录、job PID与实际命令行后，
仅向旧mixed worker `378126`发送SIGTERM。22:46:53完成保存／导出，后续自动回收逐文件校验通过。

- 成功PPO更新：**19179**。
- 完整更新对应transitions：942686208；包括最后未完成rollout的实际物理采样：942722048。
- 回收：**217项文件**，包含周期checkpoint、最终checkpoint、ONNX、合同、源码快照、TensorBoard及力矩历史。
- 回收目录：`reports/v5_mixed_recovered_20260918/`。
- archive SHA256：`44670393752c4fd2f6105582d1608503ef586a531bd0b444c97c7ef59f4e8a14`。
- 最终checkpoint SHA256：`51260d0c1518c697bbcf98d07af392b70161b401fc57d82d32f6adfd22137e30`。
- ONNX SHA256：`2b2836cad9ddaaef3b6f74bda0d289c7554d88ee2ece95df551c6001a55c6ea8`。
- ONNX验证最大绝对误差：4.77e-7；这只说明导出一致性，不是行为验收。

最终结束原因累计中，boundary为678694、takeoff_timeout为54118、nonwheel_contact为18459、fall为460。
各原因可能重叠，且没有按技能分组，不能由这些总数推导每个技能的成功率。
但它足以否定“回合长度下降必然等于越训越摔”的直接推断。

## 固定场景评测

入口：`scripts/evaluate_chassis.py`。协议`v5-flat-control-fixed-v1`。
每个checkpoint使用相同7个案例、每例8个有小幅初始姿态扰动的实例；10 s持续时间，误差统计剔除前1 s。
关闭命令／高度重采样和随机推力，actor确定性推理。评测使用本机CUDA PhysX、同一套模型及控制接口。

| checkpoint | 完整跑完 | 高度MAE（7例均值） | 零命令最大漂移 | 平移速度MAE | 旋转yaw MAE | 基础门槛 |
|---|---:|---:|---:|---:|---:|---|
| foundation最终580次 | 56/56 | 68.65 mm | 0.619 m | 0.1483 m/s | 0.1017 rad/s | 未通过 |
| mixed `model_5000.pt`，实际5001次 | 56/56 | 3.23 mm | 0.160 m | 0.0155 m/s | 0.0651 rad/s | 未通过定点漂移 |
| mixed最终19179次 | 56/56 | **0.95 mm** | **0.094 m** | 0.0282 m/s | **0.0389 rad/s** | **通过本协议** |

三个站立高度为0.29／0.305／0.32 m；平移指令±0.3 m/s，旋转指令±0.6 rad/s。
“通过”只针对这些固定低速案例和文档中的初始门槛，不能扩展成高速、台阶、跳跃或实机验收。
平移速度误差在最终权重中略大于5001次，但仍通过0.10 m/s门槛；并非所有维度都单调改善。

结论：旧训练的基础能力有明确价值，最终权重并未在该基础套件上整体退化。
新训练选择最终actor作为初始化候选，保留它的独立评测和权重以检测回归。
不能因为改了训练设计就忽略这个已经通过初级门槛的基线。

原始报告与代表轨迹：`reports/v5_checkpoint_evaluation_02/`。
第一次工程运行在跨actor重置时发现PyTorch inference tensor的原地更新限制；
改为`no_grad`后完整重跑三个候选，未使用失败运行充当最终对照。

## 新流程的工程验证

`reports/v5_v2_block_probe_01/`完成8环境、8次CPU PPO更新，随后自动执行固定评测和导出。
任务状态`budget_exhausted_gate_pending`正确表示预算结束且能力门槛尚未通过。
ONNX误差约4.62e-7；工程联通成功不等于新策略优于基线。

正式新运行使用独立合同`contracts/v5_locomotion_v2.json`，actor迁移，critic与优化器新建。
每500次更新结束训练子进程，释放其仿真资源后执行固定评测；持续保存分组误差、结束原因和高精度力矩统计。
连续通过两次才标记基础阶段完成；存在通过基线而连续三次回归时停止继续训练并保留基线。
